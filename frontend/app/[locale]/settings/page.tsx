"use client";
// Localized wrapper for settings page to avoid 404 on `/{locale}/settings`.
// Reuse the existing implementation from the non-localized route.
import SettingsPage from "@/app/settings/page";

export default SettingsPage;

