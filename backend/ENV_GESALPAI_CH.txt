# Gesalps Backend - Production Environment Variables for gesalpai.ch
# Copy this content to .env file on your VPS

# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
SUPABASE_ANON_KEY=your-anon-key-here

# CORS Configuration - Already configured for gesalpai.ch
CORS_ALLOW_ORIGINS=https://gesalpai.ch,https://www.gesalpai.ch,http://gesalpai.ch,http://www.gesalpai.ch

# Email Configuration (Optional - if using Resend)
SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_USER=resend
SMTP_PASSWORD=your-resend-api-key
SMTP_FROM=noreply@gesalpai.ch

# Service URLs (internal Docker network - don't change)
REPORT_SERVICE_BASE=http://report-service:8010
OLLAMA_BASE=http://ollama:11434

# Worker Configuration
WORKER_ENABLED=true
DP_BACKEND=custom
DP_STRICT_DEFAULT=false

# Security Settings
ALLOW_INSECURE_SUPABASE_DEFAULTS=false

# Upload Limits
MAX_UPLOAD_MB=10

